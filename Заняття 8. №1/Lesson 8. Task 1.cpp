/*
Lesson 8 | 1) Написати функцію, яка отримує номер місяця і повертає 
кількість днів у цьому місяці. Написати функцію, яка по номеру місяця 
виводить його назву. За допомогою цих двох функцій вивести на екран 
назви місяців та кількість днів у кожному місяці.
*/

#include "mylibrary.h"

int main() {
	
	for (int i = 1; i <= 12; i++) {
		cout << getNameOfMonth(i) << " ";
		cout << getNumOfDaysPerMonth(i) << endl;
	}
	return 0;
}